{
  "name": "qa-orchestrator-tooling",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test:e2e:playwright": "playwright test --config=playwright.config.ts",
    "test:e2e:cypress": "cypress run --config-file cypress.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@types/node": "^22.19.17",
    "cypress": "^14.3.2",
    "typescript": "^5.8.3"
  }
}
